1.递归
F[n] = F[n-1] +F[n-2]
缺点：不能求位次太大的数
如求第五十位的斐波那契·计算机重复计算同一个数的次数太多导致计算时间过于长。
2.循环(两种循环都可以）
1* for(i = 3, i < n, i ++ )
2* while(n > 2)
{
c = a + b
a = b
b = c
n --}
return c;
(n--原因是让 c = a + b这个赋值式只执行两次就退出循环）
